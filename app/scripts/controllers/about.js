'use strict';

/**
 * @ngdoc function
 * @name bootstrapExApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the bootstrapExApp
 */
angular.module('bootstrapExApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.isCollapsed = false ;
  });
